name: spark-delta-uc
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.14
  - httpx>=0.27
  - h2>=4.1        # enables HTTP/2 support in httpx
  - pip
  - pip:
    - unitycatalog-ai==0.4.0
    - unitycatalog-openai==0.4.0
    - unitycatalog-anthropic==0.2.0
    - unitycatalog-langchain==0.4.0
